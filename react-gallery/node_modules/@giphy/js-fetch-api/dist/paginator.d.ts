import { IGif } from '@giphy/js-types';
import { GifsResult } from './result-types';
/**
 * @hidden
 */
export declare const gifPaginator: (fetchGifs: (offset: number) => Promise<GifsResult>) => () => Promise<IGif[]>;
